// use rust_mandelbrot::views::cli_default::cli_default;

use rust_mandelbrot::views::{
    // select views
    cli_positional::cli_positional,
    // cli_default::cli_default
};

fn main(){
    // cli_default();
    cli_positional()
}